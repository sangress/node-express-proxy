{
  "name": "node-express-proxy",
  "version": "1.0.0",
  "description": "A simple yet useful express app that proxy client's requests to API server; with client routing support.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "sangress",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0",
    "http-proxy": "^1.16.2"
  }
}
